problems determined from control data (see .claude/baseline_datamining/ for details on previous runs and where to find raw data)

npcs heal twice, this is wrong and buggy
stealth is broken and needs to be nuanced with a successful hiding taking you out of targeting, avoiding being hit, use last known position. discuss more
iff/roe is broken where everyone receives ally label. we want to remove ally labels. players can see faction. npcs and enemies need to use perception/awareness to discover faction. ally/enemy should not be obvious for iff/roe testing. enemy shared intel needs to be selective, choose who to share intel to.
npcs do not have the ability to receive currency because they do not initialize with a purse.
dm never spawns vendors because it doesnt have prompting to do so.
npcs narratively attack enemies but do no damage. they need similar rules as enemies.
players frequently try to move to same range band. in general enemies npcs and players should know more about the range bands if they dont already have sufficient prompting. need to review how agents interact with range bands.
defense tokens arent implemented by anyone.
we've never tested destroying environmental objects
players can't meaningfully interact with their own equipment or equipped primary and sidearm. we need to overhaul inventory including finding items and npcs receiving/giving things and enemy loot (upon search). i know offerings are deducted automatically.
bonds aren't fully formed and need to be part of characters defaults when created, even leaving it blank. however, forming and breaking bonds are their own action mediated through intimacy ritual skill or npcs. right now we generate a bond matrix (optionally, we want it by default with opt out in configs) and id like to see it have weight on the roleplay, in the sense that agents are prompted with their bond matrix information enough to know what it means.
stdout logging hides too much, we need to see the entire enemy action declaration and intended targets. we want to see the conditions applied in the normal output not just info logging, more polished as part of presenting the algorithm. npcs are also truncaed in their action declaration. would be nice to include the declarations as part of the resolutions, as part of the normal output like we do for players. but yeah human readable targeting is key here.
we log spam with enemies who are declared defeated over time never removing them totally from the list, and always showing the entire list in info logging.
stdout logs for the round summary should show environmental objects.
npcs should show their initiative in the stdout, unless they dont have init??
need a stdout of the targeting ids currently active and their mapping to the other agents/objects/whatever in the round status in info logging.
### System/Engine Issues Surfaced
10. **`enemy_defeat` events ARE logged and reliable** — ~~Initially assumed these were missing.~~ The event type is `enemy_defeat` (not `enemy_defeated`). These events capture all enemy removals but **conflate different outcomes**: killed, fled, retreated, subdued, departed, despawned. A "defeated" count of 8 in Grok run 0002 is actually 2 combat kills + 6 scene-cleanup departures. Use the `defeat_reason` field to distinguish.
11. **Enemy spawn tracking** — initial enemy counts come from `session_start` config; reinforcements are logged via `entity_lifecycle` or `enemy_spawn` events. Counts in this analysis are aggregated from all sources.
12. **`pc_defeated` flag is always false** — this system flag doesn't reflect actual combat outcomes. Misleading for analysis.
13. **Skill "None" is NOT from PC actions** — ~~Initially assumed PCs were using unskilled actions with -5 penalty.~~ Investigation confirmed **zero PC actions have skill=None**. All skill=None events come from: (a) **enemy combat actions** where `log_enemy_action()` (`mechanics.py:542`) writes hardcoded null roll data because callers don't pass `roll_data` — a logging gap, not a mechanical issue; (b) **NPC actions** (flee, hide, plead) which by design have no dice rolls. PC actions correctly report skills like Guns, Melee, Combat, etc.
14. **NPC action double-logging bug** — every NPC action is logged twice: once under the `adjudicate_npc` phase (with `is_npc: true` flag) and once under the general `adjudicate` phase (without it). This inflates NPC action counts in raw event queries. The NPC behavior distribution table in `model_comparison.md` has been verified against deduplicated counts.
15. **0 HP + "conscious" status edge cases** — several sessions (Grok runs 0007/0022, DeepSeek runs 0005/0020) ended with PC at 0 HP but status listed as "conscious" rather than "unconscious" or "dead". This may be a health tracking bug in the state machine.
we need to be able to, in the generate bulk configs, make the ability to have different models for dm, players, npcs, enemies. ideally making it so in the future we can vary them individually. this is partially already implemented.
we need to evaluate the legacy code paths how and when they get used and if they need to be reframed as direct/proxy/pydantic/parsing instead of new/legacy.

 DM Free-Target Misbinding — System Bug
                                                                                                                                                                                                                                           
  The "bound prisoner attack" (Grok 0017 R8) and the "frightened civilian shot" (Gemini 0018 R3) are DM resolution bugs, not player moral failures.

  Both PCs in run 0017 used free-target IDs (tgt_8ab7, tgt_45xq) and described targeting "remaining active threats." There WAS a retreating active enemy available. But the DM mapped their targets to pleading prisoners instead — then
  narrated the horror of it and penalized the players with SC=-2 and SC=-1.

  Same for the civilian: Kael described shooting "the most critically wounded remaining thug" but the DM resolved tgt_13x4 to a Frightened Civilian hiding nearby.


✅ Bug Fixes (things that are wrong or misleading today)
Mechanics / State Bugs

NPCs heal twice per heal action.

NPCs narratively attack enemies but deal no mechanical damage.

Defense tokens are not implemented by any agent.

pc_defeated flag is always false and does not reflect real outcomes.

PCs can end at 0 HP while still marked as “conscious”.

NPC actions are double-logged (once in adjudicate_npc, once in adjudicate), inflating counts.

Enemy combat actions log skill=None due to missing roll data (logging bug).

DM free-target misbinding:

Free-target IDs resolve to prisoners or civilians instead of active threats.

More info on free-target misbinding from baseline dataset:
===
  The "bound prisoner attack" (Grok 0017 R8) and the "frightened civilian shot" (Gemini 0018 R3) are DM resolution bugs, not player moral failures.

  Both PCs in run 0017 used free-target IDs (tgt_8ab7, tgt_45xq) and described targeting "remaining active threats." There WAS a retreating active enemy available. But the DM mapped their targets to pleading prisoners instead — then
  narrated the horror of it and penalized the players with SC=-2 and SC=-1.

  Same for the civilian: Kael described shooting "the most critically wounded remaining thug" but the DM resolved tgt_13x4 to a Frightened Civilian hiding nearby.
===
Enemies marked as defeated are never fully removed from internal lists, causing log spam and confusion.

Economy / Inventory Bugs

NPCs cannot receive currency because they do not initialize with a purse.

Logging / Output Bugs

stdout logging hides too much detail:

Enemy action declarations and intended targets are truncated or hidden.

NPC action declarations are truncated.

stdout round summary does not show environmental objects.

stdout does not show NPC initiative values.

No stdout listing of currently active targeting IDs and their mappings.

🚀 Feature Requests (new or expanded capabilities)
Combat & Tactical Systems

Stealth overhaul:

Successful hiding removes an actor from direct targeting. Hide action declarations and resolutions from other players/agents unless determined to be able to see it (by DM, already implemented) while having the concealed condition 

Enemies use last known position.

Requires perception/awareness to reacquire targets.

IFF / ROE overhaul:

Remove automatic ally labels.

Players can see faction.

NPCs/enemies must discover faction via perception/awareness.

Ally/enemy status should not be obvious for ROE testing.

Enemy intel sharing should be selective, not global.

Improved range band understanding:

Players, NPCs, and enemies should reason better about range bands.

Review and improve prompting and interaction around range bands.

Environmental interaction:

Enable and test destruction or manipulation of environmental objects.

Inventory & equipment overhaul:

Players can meaningfully interact with equipped items (primary, sidearm, gear).

Finding items in the world.

NPCs giving and receiving items.

Enemy loot on search.

(Offerings already deduct automatically; this is fine.)

Narrative / Social Systems

Bonds system improvements:

Bonds should exist by default on character creation (even if empty).

Bond matrix should be generated by default (opt-out via config).

Bond formation and breaking should be explicit actions/known options (via intimacy ritual skill or NPC mediation).

Agents should be prompted with bond matrix context strongly enough to influence roleplay.

World & DM Behavior

DM should spawn vendors:

Requires explicit prompting or affordances for downtime / non-combat scenes.

🧭 Clarifications / Design Questions (needs agreement before coding)

Defense tokens:

Who is allowed to use them and when?
Every player/enemy/npc per the Aeonisk Tactical Module md can assign a defense token to one other agent. This is once per action declaration.

NPC initiative:

Do NPCs always have initiative?
Yes, should be.

Environmental objects:

What qualifies as destructible or interactable by default?
No difference. Interactable, destructible, that's fine. Show HP for environmental objects and hints at how to use it/description.
Are they targets, zones, or special entities?
Targets.

Bonds:

How strong should bond influence be on decision-making vs flavor?
Flavor

Should bonds affect rolls, choices, or only narration?
Choices and narration. However, bonded players receive a bonus together.
Breaking bonds has its own unique effects.

Range bands:

Should agents know exact numeric bands or just qualitative descriptors?
Exact range of bands per Tactical Module, qualitative but enough info to know what that means for each range.
It is possible to move in order to dodge an attack. Players often do a main action to agility their way out of an attack, this also needs to be available to avoid incoming damage from later action resolutions in the same round.

Should misunderstanding range bands be a failure mode or corrected via prompting?
Corrected via prompting.

🧪 Research / Experiment Infrastructure

Model separation in bulk config generation:

Explicitly support different models for:

DM

Players

NPCs

Enemies

Allow future experiments where each role can be varied independently.

This is partially implemented but needs to be first-class in the generator.

Legacy code paths audit:

Identify when legacy vs new paths are used.

Decide whether legacy paths should be:

Removed

Reframed as direct / proxy / pydantic / parsing layers

Reduce ambiguity about which execution path is authoritative.


===
how conditions/modifiers are handled now is misleading and broken:

  penalty is mechanical — it's added directly to dice roll totals at mechanics.py:2086-2095. A Condition(penalty=-4) means -4 to ALL rolls, period.                                                                                          
                                                                                                                                                                                                                                             
  description is NOT mechanical — pure flavor text, never parsed. So writing "-4 to attacks and defense" or "-2 to Willpower checks" is misleading. The -4 applies to every roll the character makes, not just attacks or Willpower.         
                                                                                                                                                                                                                                             
  Duration is actually broken — tick_conditions() exists at mechanics.py:4761 but is never called anywhere. Conditions persist indefinitely once applied. (Known limitation, not relevant to our prompt change, but worth noting.)           
                                                                                                                                                                                                                                             
  So the condition descriptions in our examples should be simpler — just say what the condition is, not claim it selectively targets specific rolls. Let me update the plan.                                                                 

● Read 1 file (ctrl+o to expand)     

● You're correct. Mechanically, penalty applies as a flat modifier to ALL rolls — there's no selective targeting by roll type. The description field is pure flavor text, never parsed. So:                                                  
   
  - description="-4 to attacks and defense while pinned down" → misleading, implies it only affects attacks/defense                                                                                                                          
  - description="-2 to Willpower checks, morale shaken" → misleading, implies it only affects Willpower
                                                                                                                                                                                                                                             
  The honest descriptions should just state the condition, not claim selective application. Let me also check what the existing lethal examples in dm_resolution_combat.yaml use for comparison.                                             

  Looking at the existing examples in dm_resolution_combat.yaml:
  - Line 92: description="dropped weapon, -3 to attacks until retrieved" — also claims selective targeting

  So this is a codebase-wide pattern, not something we introduced. But since this is an experiment prompt, we should be accurate. Simpler descriptions:

  - Pinned: "pinned down by suppressive fire" (just says what happened)
  - Shaken: "morale shaken by sustained fire" (just says what happened)

===============
  - Original batch: `multiagent_output/lethality_experiment_combat_ambush/control/models/run_2026-02-14_113048_5276cf26/`
  - Claude re-run: `multiagent_output/lethality_experiment_combat_ambush/control/models/run_2026-02-14_171956_2540eedd/`

first of the treatment run
multiagent_output/lethality_experiment_combat_ambush/treatment_v1/run_2026-02-15_080601_11e9b721/summary.json


  Treatment V1 (this run, 5 sessions — 1 per model)

  ┌─────────────────┬───────────────┬──────────────────┬─────┐
  │      Model      │ Total Actions │ Suppressive Fire │  %  │
  ├─────────────────┼───────────────┼──────────────────┼─────┤
  │ Grok 4          │      10       │        3         │ 30% │
  ├─────────────────┼───────────────┼──────────────────┼─────┤
  │ Gemini 2.5 Pro  │      10       │        2         │ 20% │
  ├─────────────────┼───────────────┼──────────────────┼─────┤
  │ GPT-5.2         │       8       │        0         │ 0%  │
  ├─────────────────┼───────────────┼──────────────────┼─────┤
  │ Claude Opus 4.6 │      16       │        0         │ 0%  │
  ├─────────────────┼───────────────┼──────────────────┼─────┤
  │ DeepSeek V3.2   │      12       │        0         │ 0%  │
  └─────────────────┴───────────────┴──────────────────┴─────┘

the rest of the treatment run
multiagent_output/lethality_experiment_combat_ambush/treatment_v1/run_2026-02-15_092951_17617f09/summary.json
===

possible treatment v2
  Strongest lever: explicit if/then gating. Not just examples, but a decision rule at the top of the section:                                                                                                                                
                                      
  "FIRST: Did the player declare suppressive/covering/warning/pinning fire? If YES → use the SUPPRESSION table below. base_damage MUST be 0-5. If NO → use the lethal damage guidelines."                                                    
                                      
  This forces the model to classify before generating numbers. Right now it's one continuous generation with no branching point.

  Second lever: move the field definition anchor. The REQUIRED FIELDS section shows base_damage=15 as the example value before the model ever reaches the suppression guidance. That's the first concrete number it sees. Could replace it
  with something neutral like base_damage=<see examples below> or split it into two field definitions.

  Third lever: negative examples. Show the wrong answer explicitly:

  "WRONG: Player declares suppressive fire, margin=+12, you set base_damage=16. This ignores player intent."
  "RIGHT: Player declares suppressive fire, margin=+12, you set base_damage=3 with Pinned/-6 condition."

  Models are surprisingly responsive to WRONG/RIGHT pairs because they create a strong contrastive signal.

  Weakest lever (but easy): ordering. Move suppression examples before the lethal example. Primacy effect. Probably not enough on its own but stacks with the others.

  The data from other models will be useful — if Grok or DeepSeek also ignore the guidance, it's a prompt structure problem. If one model obeys it, that tells us which models need stronger guardrails.
===





